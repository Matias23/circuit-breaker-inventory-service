spring:
  application:
    name: circuit-breaker-inventory-service
  threads:
    virtual:
      enabled: true
  datasource:
    # DB_CLOSE_DELAY=-1 keeps the in-memory schema alive between pooled connections.
    url: jdbc:h2:mem:inventory;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

server:
  port: 8082
